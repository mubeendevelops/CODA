Thoughts extracted from this consultation, in turn order:

{thought_list}

Predict the causal, logical, negation, elaboration, and coreference edges
between them, following the rules in your system instructions. Do not emit
temporal edges — those are already derived from turn order.

Output only the JSON object with the single top-level key "edges".
