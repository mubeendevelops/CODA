
---

Your previous response was rejected by the validator.

Validation error:
{validation_error}

Your previous response:
{previous_response}

Correct the specific problem named above and output the corrected JSON
object. Common causes: a thought id that does not appear in the list you were
given, a self-edge (source and target identical), an `edge_type` outside the
permitted five values, emitting a temporal edge (never do this — temporal
edges are rule-derived), or wrapping the JSON in markdown code fences.

Output only the corrected JSON object. No prose.
