{variant_instruction}

Fields to produce, each with the supporting thoughts a graph traversal
selected as its evidence:

{field_blocks}

Produce the JSON object described in your system instructions, containing
exactly these field keys: {field_keys}.

Output only the JSON object.
