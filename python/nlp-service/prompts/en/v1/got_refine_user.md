Field: {field_key} ({field_shape})

Supporting thoughts (the evidence this field must be built from):

{subgraph}

Current candidate (iteration {iteration}, aggregate score {score}):

{candidate}

{score_detail}

Critique this candidate against the supporting thoughts per your system
instructions, then output the revised version. If it is already correct,
say so and return it unchanged.

Output only the JSON object.
