
---

Your previous response was rejected by the validator.

Validation error:
{validation_error}

Your previous response:
{previous_response}

Correct the specific problem named above and output the corrected JSON
object. Common causes: a `text_span` that is not a verbatim substring of the
cited turn (copy it character-for-character, including disfluency), a
`turn_index` that does not exist in the transcript, a `clinical_category` or
`polarity` outside the permitted value list, or wrapping the JSON in markdown
code fences.

Output only the corrected JSON object. No prose.
