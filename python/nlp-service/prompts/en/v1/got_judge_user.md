Field: {field_key}

Supporting thoughts (the evidence this field's candidates were built from):

{subgraph}

Candidates:

{candidates}

Score every candidate on relevance, consistency and redundancy per the rubric
in your system instructions. Remember that redundancy is higher-is-worse and
is about repeating OTHER fields, not about repeating the supporting thoughts.

Output only the JSON object.
