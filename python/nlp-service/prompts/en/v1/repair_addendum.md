Your previous response did not satisfy the required output. Validation
error:

{validation_error}

Your previous response was:

{previous_response}

Produce a corrected JSON object that fixes this specific problem, following
every rule in the system instructions exactly. Output only the corrected
JSON object — no prose, no explanation, no markdown code fences.
